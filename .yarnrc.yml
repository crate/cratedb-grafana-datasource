nodeLinker: node-modules

# Block dependency lifecycle scripts by default; arbitrary postinstall code is a
# common supply-chain vector. Allow a specific package's build script when it
# genuinely needs one via `dependenciesMeta.<pkg>.built: true`.
enableScripts: false
